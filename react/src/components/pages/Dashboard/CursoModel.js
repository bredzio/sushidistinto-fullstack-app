class CursoModel {
    constructor (newId, newTitle, newContent, newPrice, newCategory) {
            this.id = newId;
            this.title = newTitle;
            this.content = newContent;
            this.price = newPrice;
            this.category = newCategory;
    }
}

export default CursoModel;